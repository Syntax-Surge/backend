// // models/index.js
// const { sequelize } = require('../config/db');
// const Sequelize = require('sequelize');

// // // Import all models
// const User = require('./user.model')(sequelize, Sequelize.DataTypes);
// // const Budget = require('./budget.model')(sequelize, Sequelize.DataTypes); // Example

// // // Define relationships here
// // User.hasMany(Budget, { foreignKey: 'userId' }); // One-to-Many relationship
// // Budget.belongsTo(User, { foreignKey: 'userId' }); // Reverse relation

// const db = {
//   sequelize,
//   Sequelize,
//   User  
// };

// module.exports = db;
